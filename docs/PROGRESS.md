# TBH HUD — Progress vs. the plan

Living status tracker. **The plan** is [PRD.md](PRD.md) (goal list + phased roadmap + acceptance criteria).
**The trace over time** is [improvement.log](../improvement.log); **what's next** is CLAUDE.md §Next. (GOAL.md / SESSION-GOAL.md are historical.)
Keep this file honest and current — update it at the end of every working session.

Legend: ✅ done · 🟡 partial · ⛔ deferred/blocked (reason given)

_Last updated: 2026-06-12, after session 24 (v1.0.20 + v1.0.21). **v1.0.21 — English/简体中文 language switch:
a 中/EN header toggle (persisted, applied pre-paint) flips the whole HUD; game terms (item/hero/stage/rune/rarity/
difficulty names) come from the game's OWN zh-Hans localization bundle (extract_localization_zh.py → i18n_zh.min.js),
the HUD's chrome is hand-translated on top, applied by a guarded MutationObserver DOM translator that restores English
on toggle-back; ~100% coverage on Overview/Runes, long fragmented help-text + mini.html are the known tail. v1.0.20 —
stages decode the difficulty band: the meaningless "Act 12-5" → "Act 3-5 · Torment" (4 bands Normal/Nightmare/Hell/
Torment × 3 acts × 10, calibrated from the game's Difficulty_* keys + the 4 Soulstones + the 116-stage offline table);
the Crew board re-decodes from the numeric maxStage key so older clients show the new form. Both shipped to web +
desktop (installer verified to contain the i18n file via asar list).** Earlier — sessions 18–23 (v1.0.11 → v1.0.17): **v1.0.17 — GOAL-v1.0.16 P5+P6, the wave
is COMPLETE (P1–P6): Codex recipes from 7 game tables (5 newly extracted) — per-item "the Cube can make this"
(crafting ingredients/unlock levels/pool sizes + synthesis bands with the game's own rule quoted) and "the Cube
uses this", the Advisor Cube reference (8 localized categories, sub-recipes ✓/🔒 vs the player's Cube level),
odds omitted everywhere (weights exist raw but compose in game code); opt-in enchant crowd-calibration (3-field
tuples only, server aggregate counters, no identifiers); crew API hardened (rate limiting all endpoints, prune
with a 7-day server floor, "Remove my data" button) and REDEPLOYED with a 10/10 live smoke test; the Targets
panel (gold / rune / locked-gear / stage) with ETAs from the player's own measured rates only.** **v1.0.16 — the power-user wave (GOAL-v1.0.16
P1–P4): Loot filters/search/sort/date-headers/paged-counts + CSV/JSON export + the honest "boxes Steam hasn't
delivered" count (from the player's own `items is empty` log lines); the blue-chest tracker that MEASURES the
player's own Stage-Boss-Box intervals (no rate/cooldown exists in the game files — file-verified, findings docs
committed) with same-stage vs after-switch medians and sample sizes; Inventory search/type/sort/enchanted/equipped
controls + neutral two-item Compare + the provable "Safe to let go" redundant-duplicates finder (both engines +
verify_save invariants, hand-verified); desktop mini-HUD (always-on-top, save-fed over IPC, bounds+opacity
remembered) + ⚙ Settings (density/motion/default-tab/alerts) + keyboard shortcuts (1–9/0, ←/→, /).** **v1.0.15: update downloads fixed — differential ("patch") downloads were fetching ~the whole 79 MB as thousands of sequential range requests (solid-compressed NSIS); now one full stream, no blockmaps shipped, banner shows MB + live speed.** **v1.0.14: app version visible in the footer on every platform (game-v vs app-v made explicit with tooltips); Loot's Steam-boxes section explains its real Player.log provenance and boxes click through to their Codex contents; demo mode now isolates Player.log data (real data can't render under the SAMPLE badge).** **v1.0.13: Crew member pop-up with the COMPLETE shared Stat List (the old payload's top-6/32-char caps were why it looked cut) + side-by-side member compare (defaults to you, larger value highlighted); Advisor "At a glance" cards + per-swap inherent-stat comparison from GearInfoData; click-through everywhere (items/trophies/gear/loot/stones → Codex, rune cards → per-level table, Stat-List lines filter the rune grid).** v1.0.12: dark theme redesigned after owner feedback (slate-indigo/periwinkle counterpart of the light design, AA re-verified). **v1.0.11: visual revamp — light (default) + dark themes on one semantic-token design system (WCAG AA verified programmatically in both; rarity hues canonical), the uneven-boxes fix (shared grid-track tokens, auto-fill, one spacing/radius scale; 11 tabs × 3 widths × 2 themes = 0 overflow), the game's grouped Stat List (Exploration/Combat) calibrated line-by-line against the in-game panel (rune aggregate proven; game's own AccountStat_* wording; uncalibrated lines omitted) on Runes + shared/ranked in Crew, and public patch notes (CHANGELOG.md + in-app ✨ What's new with first-run dot).** Earlier: v1.0.4 per-stage gold/hr + Advisor + Crew (Vercel+Neon); v1.0.5 visible update checks; **v1.0.6 fixed the auto-updater that had NEVER worked in any shipped build** (≤1.0.5 installs need one manual reinstall); v1.0.7 equip-gating + per-stage XP/hr + Enchanting workshop; v1.0.8 crew play-hours + gear-only tier flex + loot origin inference; v1.0.9 online-vs-offline measured; v1.0.10 rune Stat List + crew version tags. Auto-update chain observed end-to-end on real installs three times (1.0.6→7→8→9)._

## Goal list (PRD §3) — where each goal stands
| # | Goal | Status | Notes / where it landed |
|---|------|--------|--------------------------|
| 1 | Inventory: names, rarity, icons, level, enchants (correct counts) | ✅ | + inherent stats & unique mods. **+ full-catalog Codex (6177 entries, owned-marked, drop sources/box contents).** 100% names/icons. |
| 2 | Gold: balance, lifetime, gold/hr (live+session) **+ per-act ranked** | ✅ | Balance/lifetime/session ✅ + **gold by source ✅** (session 12). **Per-STAGE ranked gold/hr ✅ (session 13, SAVE-ONLY — no memory lane needed):** the combat sub-counter (Type 2/Sub 1, calibrated) delta is attributed to `currentStageKey` over CLEAN single-stage intervals from the HUD's own snapshot history + game backups; offline gold auto-excluded (it's in Sub 2/3). Measured on the real save: Act 2-8 ≈ 84.9k/hr vs Act 1-6 ≈ 36.3k/hr. Ranked on Trends + Lifetime + Overview one-liner; honest caveats (clean intervals, includes idle, sharpens over time). |
| 3 | XP: xp/hr (live+per act); per-hero XP **+ ETA to next level** | 🟡 | **Per-hero XP-to-next-level ✅** (session 5) + **per-hero time-to-next-level ETA ✅** (session 11). **Per-stage kills/hr ✅ (session 13)** — measured alongside per-stage gold/hr, the honest save-only proxy for farming pace. Per-stage *XP*/hr stays ⛔: the save has no calibrated lifetime-XP aggregate to delta (calibrate-or-omit). |
| 4 | Clear time (per run, per stage/act) | ⛔ | Needs live combat (memory lane). Deferred for ban-safety. |
| 5 | Live combat: DPS, dmg, mobs, gold/s, xp/s, overlay | ⛔ | Memory lane → ban risk. **Deliberately not built.** |
| 6 | Per-hero perf + "who's carrying" + **WHY (source breakdown)** | ✅ | Full source breakdown shipped: per deployed hero, factual stats by Base/Gear/Tree + account-wide runes/pet — **+ a summed Total row (session 13)** and the new **Advisor tab**: provable gear-gap upgrades (same GEARTYPE, strictly better, greedy 1:1), cheapest-first rune plan within gold, open enchant slots on the deployed party. Live DPS share ⛔ (memory). |
| 7 | Loot timeline: timestamped, rarity+source, **rare alerts** | 🟡 | Timeline + offline rewards + **rare-drop alerts** (Legendary+ ★ + opt-in notify) ✅. **v1.0.16: full controls** — rarity/origin filters, search, 3 sorts, date headers, paged with honest counts (store 120→500), **CSV/JSON export**, and the calibrated "boxes Steam hasn't delivered" note (counted from the player's own `CreateSteamItem … items is empty` lines). Per-source attribution ⛔ (live drops not logged by the game; Cube-consumption inference from v1.0.8 stands). |
| 8 | Steam Market value for tradeables | ⛔ | Steam Inventory Service throttled/empty in this build (`CreateSteamItem … items is empty`). |
| 9 | Lifetime stats: kills, gold **+ other aggregates** | ✅ | Total kills + lifetime gold (both calibrated) + **calibrated kills-by-monster** (Type-0 subs sum == total kills). **Per-difficulty completions REMOVED in session 7** — Type 16 is uncalibrated and was disproven by the live save (Normal-only at Act 2-10 yet claimed Nightmare/Hell/Torment); omitted per the golden rule. All other unconfirmed aggregate Types omitted, not guessed. |
| 10 | History / trends over time | ✅ | Trends tab from rolling save backups (session 2). |
| 11 | Runes: 197-node tree, names, leveled, cheapest-next | ✅ | Rune tab. |
| 12 | Blue-chest / cooldown tracker | 🟡 | **v1.0.16: MEASUREMENT shipped** — every save-diffed Stage Boss Box (the blue chest, RARE STAGEBOX) is logged {time, stage, playHours}; the Loot panel shows the player's OWN same-stage and after-stage-switch gap medians with sample sizes (played-time gaps; closed-game periods flagged). The 12-min cadence itself stays **unasserted** — it's not in the files (BLUE-CHEST-DROP-RATE-FINDINGS.md) — so no auto-ping until the player's own data proves a cadence. Box counts still surfaced. |
| 13 | Animated hero portraits | ✅ | 6 GIFs. |
| 14 | Private friends leaderboard (optional) | ✅ | **Crew tab (session 13) — OPT-IN, privacy-safe:** shared crew-code + display name (no accounts); "Share my progress" OFF by default; pushes ONLY calibrated brag-stats (max stage, lifetime gold, kills, gold, top-3 hero levels, runes, Legendary+ count, play hours — never the save); ~30s polling, rank-by selector, latest achievement from each member's own snapshot deltas, gap-to-you, copy-invite-code. Backend: Vercel serverless (`api/progress`, `api/leaderboard`) + Neon Postgres (`tbh_crew_*`), live-verified end-to-end. Non-opt-ins keep the zero-upload experience. |
| 15 | Premium UI that beats both competitors | ✅ | **11 tabs** (incl. Advisor + Crew). **v1.0.11: full visual revamp** — semantic-token design system with **light (default) + dark themes** (🌙/☀️ toggle, persisted; WCAG AA programmatically verified in both; rarity hues canonical), uniform cards on shared track/spacing/radius tokens (the "uneven boxes" complaint fixed), **fully responsive** (0 overflow on all 11 tabs at 375/768/1280 in BOTH themes). **v1.0.16: power-user reach** — Loot + Inventory get Codex-grade controls, item Compare, settings (⚙ density/motion/default-tab), keyboard shortcuts, and the **desktop mini-HUD** (the always-on-top strip tbh-meter had — save-based, ban-safe). |
| 16 | One-click installer + hosted browser version | ✅ | NSIS installer published per release (Latest = **v1.0.19**; direct download in README; no blockmap since v1.0.15 — differential updates disabled for speed) + **GitHub Pages live** (HTTPS): https://revenantcabal-rgb.github.io/taskbarheroburat/ + **the owner's Vercel project auto-deploys on push** (https://taskbarheroburat.vercel.app/). |
| 17 | Auto-update (electron-updater) | ✅ | **Actually working from v1.0.6** — session 13 discovered the chain had NEVER worked in any shipped build: electron-updater was a devDependency, so every installer v1.0.0–v1.0.5 shipped without the module and the startup `require` failed silently (proved via --inspect on the installed app). Fixed (moved to `dependencies`), verified in the packaged build (real check round-trips). Plus v1.0.5's UX: "↻ Check for updates" button with visible states + 4h periodic re-check + app version in footer. Installs of ≤v1.0.5 need one manual reinstall (README warns). |
| 18 | Patch resilience (graceful fallback, fast fixes) | 🟡 | Save-only is the mode; rebuild pipeline handles data patches; decrypt errors handled. |
| 19 | Read-only (never write game/save/memory) | ✅ | Re-audited session 2 — zero writes/injection. |
| 20 | No fabricated data | ✅ | Every label calibrated; honest fallbacks; deferrals over guesses. |
| 21 | GitHub home `revenantcabal-rgb/taskbarheroburat` | ✅ | Local = remote each push. |

## Phased roadmap (PRD §6)
| Phase | Status | Notes |
|-------|--------|-------|
| 1 — Data foundation | ✅ | decrypt+parse+analytics; calibrated DB; corrected counts; gold/hr. |
| 2 — Full game art | ✅ | 535 item + 39 rune icons. |
| 3 — Premium UI | ✅ | multi-tab dashboard. |
| 4 — Live telemetry | ⛔ | Memory lane → ban risk. Deferred indefinitely unless a provably-safe read exists. **Session 13 shrank what it's needed for:** per-stage gold/hr + kills/hr are now measured SAVE-ONLY (see goal #2); only live DPS / clear-time / per-hero DPS share remain memory-lane-only. |
| 5 — Loot timeline + alerts + market + blue-chest | 🟡 | Timeline + boxes + offline rewards + rare-drop alerts ✅; market ⛔; blue-chest ⛔. |
| 6 — History / trends | ✅ | Trends tab (session 2). |
| 7 — Packaging + distribution | ✅ | NSIS installer built; GitHub Pages live (HTTPS); electron-updater wired + Release v1.0.0 published. winCodeSign symlink fixed via signAndEditExecutable/verifyUpdateCodeSignature=false. |
| 8 — Friends leaderboard | ✅ | **Shipped session 13 (v1.0.4)** — opt-in Crew tab + Vercel/Neon backend (see goal #14). |

## Deferred / will-not-build (golden-rule decisions)
- **Live combat / DPS / per-act gold/hr / clear time** — require reading game memory → CodeStage (`[ACTk]`) ban risk. Not built; save+log lanes cover what's safe.
- **Steam Market value** — Steam Inventory Service is throttled/empty in this build.
- **12-min blue-chest timer** — cadence not in the tables (DropCooldown has no 720s). Box counts shown instead.
- **Stage-box drop contents** — ✅ now SHIPPED in the Codex: DropKey→DropInfoData→ItemGroupInfoData→member EN names ("Box can contain […]") + reverse "Drops from […]". The Korean ItemGroup `GroupName` is deliberately omitted (never guessed); per-source drop *rates* still not shown — **v1.0.17 precision note:** the raw DropInfoData DOES carry per-entry integer Weights, but their composition semantics (EachDropOneWeight + HeroKeyCondition) live in game code → membership only, odds never quoted (calibrate-or-omit; recipe pools follow the same rule).
- **Codex recipes** — ✅ SHIPPED v1.0.17 (was backlog): crafting + Cube synthesis per item, the Cube reference panel, offerings/extraction baked. "Set bonuses" will NEVER ship — item sets don't exist in this game (verified).
- **Hero XP-to-next-level + time-to-next-level** — ✅ BUILT: `LevelInfoData` curve calibrated (session 5) + a **time-ETA** (session 11) from the XP/hr **measured this session** (not the memory lane). Only per-*act* xp/hr (which stage XP came from) stays deferred.

## Phased roadmap addendum (owner's GOAL.md)
| Phase | Status | Notes |
|-------|--------|-------|
| A — full-catalog Codex + audit | ✅ | New virtualized Codex tab (6177 entries), filters/search/sort, owned-marked, per-entry detail incl. drop sources/box contents; `audit_catalog.js` asserts 100% name+icon. |
| B — offline-rewards card | ✅ | Overview card: live idle since last save + last collection (gold+rate from Player.log) + cap countdown. Cap **learned from the user's own logs** (no game table holds it → no assumed 8h). TZ bug caught: ticks are LOCAL, idle anchored on file UTC mtime. |
| 7 — real run + packaging | ✅ | `npm start` runs clean; installer `TBH-HUD-Setup-1.0.1.exe`; Pages live (HTTPS); electron-updater wired + Releases v1.0.0/v1.0.1 published. |
| 5b — responsiveness + XP-to-next + Vercel-ready (session 5) | ✅ | Mobile/tablet/desktop responsive (0 overflow); real hero XP-to-next (killed the `level/20` placeholder); cache-bust `?v=`; vercel.json + index.html; v1.0.1 shipped. |
| P1-P5 — data honesty + UX (session 7, v1.0.2) | ✅ | **P1** removed the fabricated per-difficulty completions (Type 16, disproven by the Normal-only save) → engines/renderer/demo/README; verify_save.js now asserts data honesty. **P2** stages decode to "Act X-Y" + real StageName (DB.stages baked) everywhere; no raw-key leak. **P3** plain-language "Who's carrying" (no Σ; labeled bar + tooltips + hint). **P4** baby-simple 3-step connect screen w/ copy-path + a Disconnect/Change-folder control. **P5** friendly no-save placeholder + gate polish. Verified Node + headless, 0 errors. |
| UX + the 3 owner questions (session 8, v1.0.2) | ✅ | **Multi-user:** confirmed it's a static client-side reader (no account/server; anyone uses their own save independently, nothing uploaded) + added a "No account, no sign-in" reassurance line; Disconnect now resets loot tracking so switching saves is a clean slate. **Auto-update:** made it VISIBLE — electron-updater events → an in-app "Update ready · Restart" banner (autoInstallOnAppQuit too); web is always current on load. **Uninstall:** confirmed the NSIS uninstaller + Add/Remove Programs entry (builder-debug: WriteUninstaller/uninstaller.nsh) + polished display name & shortcuts; installer rebuilt. README FAQ added. |
| Gold-by-source + farming insights (session 12, v1.0.3) | ✅ | **Gold by source** on Lifetime (Type 2 subs, sum-validated: From combat 99.5% / Other 0.5%; "Cube gold" honestly left in the tiny "other" bucket, not fabricated). **Per-monster base rewards** (RewardGold/RewardExp → DB.monsters {n,g,x}; "Ng/Nxp each (base)"). Honest **"best farming stage"** answer: not derivable (no StageInfoData; per-stage rate = memory lane) → use per-monster rewards + Trends. **Bug fix:** `el()` dropped-sibling bug had hidden the Owned-by-rarity bar/legend since session 7 → `frag()`. **Electron smoke-tested** (npm start clean). Version → 1.0.3. |
| Per-hero level ETA (session 11, v1.0.2) | ✅ | **Time to next level** per hero (cards + roster column): XP remaining ÷ XP/hr MEASURED from the session (cumulative-XP delta since connect; survives mid-session level-ups). Honest states (measuring… / not gaining XP / ≈ time); Party-tab note that it's the player's real pace. No fabricated rate. |
| Styling + solidify UX (session 11, v1.0.2) | ✅ | **Fixed blank-first-paint bug** (active tab stayed hidden on first connect/demo → render() now un-hides it; affects browser + Electron). Added keyboard **:focus-visible** rings (accessibility). Fixed Overview suggestion-strip layout (was mashed inline); snappier tab fade. Full visual audit across all 9 tabs + connect screen (desktop + mobile) — cohesive; 0 horizontal overflow @375. |
| Connection fixes + flex + honest DPS (session 10, v1.0.2) | ✅ | **Refresh-boot FIXED:** handle persisted in IndexedDB + re-attached on reload (auto-resume or 1-click Reconnect); Disconnect clears it. **Blocked-folder FIXED:** "Trouble connecting?" help + universal drag-and-drop of the .es3 (bypasses Chrome's AppData blocklist). **Flex:** Overview "Flex card" (calibrated brag stats + copy-to-share); reframed "Who's carrying" to answer "who hits hardest" honestly (no live DPS — needs the memory lane = ban risk). **Demo:** "See a demo" + amber "SAMPLE — not your data". **Market items:** not calibratable (no origin flag; trade stash empty; Steam inventory throttled) → honest note, no guessing. |
| Tips + Loot honesty + session (session 9, v1.0.2) | ✅ | **NEW Tips tab (9th)** + Overview suggestion strip: calibrated "Suggestions for you" (unspent ability points, empty gear slots, cheapest AFFORDABLE rune upgrade vs gold — all 663 rune costs verified as Gold — stash full, bench heroes behind, stagnation from trends) that refresh each read; 10 "Game tips" incl. the owner's; honest note that live clear-time/per-map optimizations need the memory lane (not built). **Loot:** craft-vs-drop is NOT calibratable (both mint new UniqueIds; no origin flag; CraftingRecipe/SynthesisRecipe both output Gear) → renamed to "New items" with an honest note + real Cube level; added dual **local + UTC** timestamp columns (timeline + offline rewards). **Session:** verified full re-parse each read = no stale data, new heroes appear correctly; fixed gold/hr to use lifetime gold (spend-proof). |
| v1.0.11 — visual revamp + grouped Stat List + patch notes (session 18) | ✅ | **Themes:** one semantic token contract under `:root[data-theme=light\|dark]`; light designed first and DEFAULT (pre-paint head script; `tbh_theme` persisted; header 🌙/☀️ toggle); dark re-mapped from the navy palette (dim text lightened for AA); zero raw hex in component CSS (color-mix tints); WCAG AA all text/surface pairs in both themes (test/_contrast.js, programmatic); rarity hues canonical, lightness-tuned on light. **Boxes:** shared `--track-*` grid tokens + auto-fill (short rows keep card width) + one `--s*`/radius scale + flex-column cards; 11 tabs × {375,768,1280} × {light,dark} = 0 horizontal overflow (66/66). **Stat List:** calibration PROVED the in-game grouped panel is the rune aggregate (live save matched the screenshot 8/9 exactly; the 9th bracketed 10%→16% through 11% by the game's own backups; passives/pets ruled out); wording = the game's own `AccountStat_*` templates; %-templates render value/10 (3 confirmations), flats raw (6); `statListFull` in BOTH engines + verify_save assertions; grouped panel on Runes, `{k,v}` payload (server-whitelisted, 9 keys) + per-member grouped render + rank-by-stat in Crew; uncalibrated effects OMITTED from the grouped view (no category table exists — all 45 game tables enumerated). **Patch notes:** CHANGELOG.md (public) + in-app ✨ What's-new modal with `tbh_lastSeenVersion` dot. Engines in-browser parity verified (identical 9 lines + key numbers); Electron smoke clean; installer + latest.yml + blockmap rebuilt at 1.0.11. |
| v1.0.5–v1.0.10 — update UX; updater root-cause fix; equip-gating + XP/hr + Enchanting workshop; crew hours/tiers + loot origins; online-vs-offline; rune Stat List (sessions 13b–17) | ✅ | **v1.0.6 is the watershed:** every installer v1.0.0–v1.0.5 shipped WITHOUT electron-updater (devDependency — never packed; silent no-op), proven via --inspect on the installed app and fixed by moving it to `dependencies`; the full chain (check → download → install-on-quit) has since been observed on real installs three times. Plus: visible update button + 4h re-check (1.0.5); Advisor equip-gating + per-stage XP/hr + Enchanting workshop (1.0.7); crew play-hours + gear-only tier flex + loot origin inference (1.0.8); measured online-vs-offline + offline-proof rates (1.0.9); rune Stat List on Runes tab + crew (1.0.10). Every feature calibrated against both real saves; engines kept in parity; verify_save extended each release. |
| v1.0.4 — history + per-stage rates, Advisor, Crew, ship everywhere (session 13) | ✅ | **F1:** HUD-own snapshot history (IndexedDB `tbh-hud-history`, separate from the `handles` store; append per save read in `onBytes`, load on startup, merge with game backups, cap 2400, cleared on Disconnect) + **per-stage gold/hr & kills/hr** (combat counter over clean intervals) → Trends ranking, Lifetime measured answer, Overview one-liner. **F2:** Advisor tab (gear-gap finder hand-verified vs the full gear dump — exactly 1 provable gap in the test save; rune plan priced from `DB.runes[].lv`; enchant slots) + Party Total row; engines in PARITY (inline output == Node output on the same save). **F3:** Crew tab + `api/progress` + `api/leaderboard` on Vercel (tbh-crew-api) + Neon (`tbh_crew_*`); DATABASE_URL in Vercel env only; CORS allowlist; achievements from snapshot deltas; demo-mode sharing disabled. **Ship:** verify_save PASS (new invariants), Electron clean, ?demo 11 tabs / 0 errors / 0 overflow @375, pushed to GitHub (Pages + the owner's Vercel project auto-deploy), release v1.0.4 published, crew API smoke-tested live (join/improve/board). |

## Current health (v1.0.4)
- **Engine parity:** the inline (browser/Electron) engine and the Node mirror produced identical numbers on the committed real save — same gear gap, same per-stage rates (84,911 / 36,313 g/hr), same rune plan (7 steps), same 19 open-enchant items. `verify_save.js` asserts invariants for every v1.0.4 fn and PASSES.
- **Privacy:** crew sharing is OFF by default; only the listed brag-stats leave the device, only after opt-in; demo mode hard-disables sharing. The no-account zero-upload default is intact for everyone else.
- **History honesty:** per-stage rates use only CLEAN single-stage intervals of the calibrated combat-gold counter; offline gold is excluded by construction; caveats (includes idle, sharpens over time) are stated in the UI.

## Current health (v1.0.3 and earlier)
- **DATA HONESTY (P1):** nothing on any tab claims progress the save doesn't support. The fabricated per-difficulty completions are gone; `verify_save.js` PASSES new assertions vs the LIVE save (no `perDifficultyCompletions`; only the whitelisted calibrated aggregates surface; kills-by-monster sum == totalKills; the Type-16 disproof is printed).
- **No console errors** vs demo + standalone (?codex) + the real save fetched through the live pipeline, across all **11 tabs**; live-save engine path verified by `verify_save.js` (Node + browser parity asserted number-for-number).
- **Stages (P2):** decode to "Act X-Y" + real StageName everywhere (Overview/Lifetime/Trends); verified 1101→Act 1-1 Pasture, 1208→Act 2-8 Sacred Tomb, 1210→Act 2-10 Pharaoh's Underchannel; no raw stage-key leak.
- **Fully responsive:** 0 horizontal overflow at 375 / 768 / 1280; the new connect screen + no-save placeholder wrap cleanly on mobile (375).
- Read-only confirmed; data calibrated (audit 100% over 6177; DB rebuilt with `stages`; Node+browser parity); local = remote.
- **Multi-user / privacy:** static client-side reader — every visitor connects their OWN local save in their OWN browser (File System Access API); no account, no server, nothing uploaded → unlimited independent users, zero shared state. Disconnect resets loot tracking so switching saves is a clean slate.
- **Auto-update:** desktop app checks GitHub releases on launch, auto-downloads a newer version, and shows an in-app "Update ready — Restart" banner (applies on next quit regardless). Publishing a release pushes it to everyone. Web is always current on load.
- **Uninstall:** the NSIS installer registers in Windows Add/Remove Programs as "TBH HUD" (confirmed via builder-debug: WriteUninstaller + uninstaller.nsh); per-user, never touches the game.
- **Distribution:** WEB current on every push (Pages, DB at `?v=1.0.3`). **DESKTOP v1.0.3 is PUBLISHED** — the Latest GitHub release (installer + latest.yml + blockmap); existing v1.0.0/v1.0.1 installs auto-update; **direct download linked in the README**. Electron app smoke-tested (`npm start` clean: 4 procs, 0 stderr). Vercel-ready (not yet deployed). (The 1.0.2 line was built but never published; 1.0.3 supersedes it.)
- **Confidence: 9/10.** Deduction: the native folder picker / refresh-reconnect / drag-drop are logic-+-demo-verified, not driven on real hardware here (need owner/friend to confirm in the wild); the installer is unsigned (SmartScreen prompt) and wasn't run through a clean-machine install→update cycle here.

## Next step
**Shipped — v1.0.17 everywhere: the GOAL-v1.0.16 wave is COMPLETE (P1–P6).** Web (Pages + the owner's Vercel project auto-deploy on push), desktop release v1.0.17 (Latest; auto-update from v1.0.6+), crew API REDEPLOYED with hardening (rate limits + prune + enchant-report; live smoke 10/10). Backlog for the next session:
- **Promote crowd-calibrated enchant categories when the data lands:** `tbh_enchant_reports` now collects opt-in (gt, stone, rolled-stat) tuples; when a gt (GLOVES/BOOTS/RING/AMULET/EARING/BRACER/offhands) has multiple consistent confirmations matching the stone's own fx table, promote it in gtGroup/gtGroupD (BOTH engines) + re-verify. One in-game enchant per type on the owner's save calibrates directly too.
- **Calibration awaiting a human eye:** the rune Stat List vs the in-game panel got its screenshot proof (v1.0.11) — nothing else pending eyes; the blue-chest tracker accumulates the player's own intervals (sample sizes shown in-app).
Optional follow-ups:
- **Move the crew API under the owner's `taskbarheroburat` Vercel project** (attach Neon storage → auto-injects `DATABASE_URL` → change the `CREW_API` constant; ONE canonical API at a time or crews split). **Sign the installer** (cert, SmartScreen). **Clerk auth** on the crew API. Mini-HUD extras: tray icon + global hotkey. **Friends on ≤1.0.5** still need their one-time reinstall. The owner's untracked Playwright scaffolding (playwright.config.js + tests/) could host real browser tests.
- To ship the NEXT version: bump `package.json` + `APP_VERSION` + `?v=` cache-bust + CHANGELOG (const + .md), `npm run dist`, then `gh release create v<ver> dist/TBH-HUD-Setup-<ver>.exe dist/latest.yml --latest` (no blockmap).
See [improvement.log](../improvement.log) · CLAUDE.md.
